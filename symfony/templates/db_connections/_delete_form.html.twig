<form method="post" action="{{ path('app_db_connections_delete', {'id': db_connection.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ db_connection.id) }}">
    <button class="btn">Delete</button>
</form>
